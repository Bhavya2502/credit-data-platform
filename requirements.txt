# Credit Data Platform — Python dependencies
# Pinned loosely (compatible-release) so security fixes land without breaking APIs.

# ── Core data handling ───────────────────────────────────────────────
pandas~=2.2
pyarrow~=17.0            # Parquet read/write
duckdb~=1.1              # local analytical engine
duckdb-engine~=0.13      # SQLAlchemy dialect (dbt/tooling compatibility)

# ── Cloud storage & warehouse ────────────────────────────────────────
boto3~=1.35              # Cloudflare R2 (S3-compatible API)
s3fs~=2024.10            # read Parquet directly from R2

# ── Fetching ─────────────────────────────────────────────────────────
httpx~=0.27              # HTTP client with retries/timeouts
tenacity~=9.0            # backoff/retry policy
beautifulsoup4~=4.12     # HTML parsing
lxml~=5.3

# ── Document extraction (archetype D) ────────────────────────────────
pdfplumber~=0.11         # text-layer PDF tables — first choice
camelot-py~=0.11         # ruled-table extraction — second choice
pymupdf~=1.24            # page rendering for vision extraction; 2nd text engine
opencv-python-headless~=4.10   # camelot dependency

# ── Config & validation ──────────────────────────────────────────────
pyyaml~=6.0
pydantic~=2.9            # source-config validation
pandera~=0.20            # dataframe schema validation

# ── Observability ────────────────────────────────────────────────────
structlog~=24.4          # structured logging

# ── Dev / test ───────────────────────────────────────────────────────
pytest~=8.3
ruff~=0.7

# Deferred until needed (documented in PLATFORM_PLAN.md §5):
#   docling          — advanced document layout parsing
#   openai           — OpenRouter client for LLM-assisted extraction (archetype H)
#   playwright       — browser automation (archetype F)
#   dbt-duckdb       — once silver exceeds ~20 tables
